name: Test impact CI
on:
  push:
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: impact-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  rust:
    name: Rust and selector checks
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - name: Set up Rust
        run: |
          rustup toolchain install 1.92.0 --profile minimal --component rustfmt --component clippy
          rustup default 1.92.0
      - run: cargo fmt --check
      - run: cargo clippy --locked --all-targets -- -D warnings
      - run: cargo test --locked
  selected-tests:
    name: Selected tests
    needs: rust
    uses: ./.github/workflows/java-tests.yml
    with:
      mode: selected
  full-tests:
    name: All tests
    needs: selected-tests
    if: ${{ !cancelled() }}
    uses: ./.github/workflows/java-tests.yml
    with:
      mode: full
  compatibility:
    name: Version compatibility
    if: ${{ github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') }}
    uses: ./.github/workflows/compatibility.yml
  container-tests:
    name: ${{ matrix.service }} container
    if: ${{ github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        service: [Kafka, Redis]
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: ./.github/actions/java-build
        with:
          tool: maven
      - name: Run container test
        env:
          TEST_CLASS: ${{ matrix.service }}Test
        run: mvn -B -ntp -f projects/containers/pom.xml "-Dtest=$TEST_CLASS" test
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: container-${{ matrix.service }}-reports
          path: projects/containers/target/surefire-reports/
          retention-days: 14
